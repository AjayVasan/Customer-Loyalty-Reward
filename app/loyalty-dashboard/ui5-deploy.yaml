# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.0"
metadata:
  name: loyaltydashboard
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateVersionInfo
      configuration:
        archiveName: loyalty-dashboard
        relativePaths: true
        additionalFiles:
          - xs-app.json
    - name: ui5-task-zipper
      afterTask: generateCachebusterInfo
      configuration:
        archiveName: loyaltydashboard
        relativePaths: true
        additionalFiles:
          - xs-app.json
resources:
  configuration:
    propertiesFileSourceEncoding: UTF-8
